# Free tier instance environment
# Copy to .env and fill in values

# Domain
DOMAIN=sphoto.arturf.ch

# Immich version
IMMICH_VERSION=release

# Database password (generate with: openssl rand -hex 16)
DB_PASSWORD=CHANGE_ME

# Upload location (optional - defaults to ./uploads)
# UPLOAD_LOCATION=/mnt/storage/sphoto-free/uploads
