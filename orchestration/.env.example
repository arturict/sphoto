# =============================================================================
# SwissPhoto Environment Configuration
# Domain: arturferreira.tech
# =============================================================================

# Immich Version
IMMICH_VERSION=release

# E-Mail für Let's Encrypt SSL-Zertifikate
ACME_EMAIL=admin@arturferreira.tech

# Traefik Dashboard Auth (generiere mit: htpasswd -nb admin YOUR_PASSWORD)
# Beispiel: admin:$apr1$xyz123...
TRAEFIK_AUTH=admin:$apr1$REPLACE_WITH_HASH

# =============================================================================
# Stripe Integration
# =============================================================================
STRIPE_SECRET_KEY=sk_live_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# Admin API Key (für manuelle Instanz-Verwaltung)
ADMIN_API_KEY=GENERATE_A_SECURE_KEY_HERE

# =============================================================================
# E-Mail (für Welcome-Mails etc.)
# =============================================================================
SMTP_HOST=smtp.example.com
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# =============================================================================
# Stripe Price IDs (ersetze mit deinen echten IDs aus Stripe Dashboard)
# =============================================================================
# STRIPE_PRICE_BASIC=price_xxx
# STRIPE_PRICE_STANDARD=price_xxx
# STRIPE_PRICE_PRO=price_xxx
# STRIPE_PRICE_POWER=price_xxx
